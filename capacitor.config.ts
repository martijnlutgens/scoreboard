import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'nl.lutsoft.scoreboard',
  appName: 'scoreboard',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
